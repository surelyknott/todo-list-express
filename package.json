{
  "name": "rap-name-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js", // we specify that the main entry point of our application is the server.js file, which contains the code for setting up and running our Express server
  "scripts": {
    "start": "node server.js", // we define a start script that runs the server.js file using Node.js, which allows us to start the server by running 'npm start' in the terminal
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "ejs": "^3.1.6",
    "express": "^4.22.1",
    "mongodb": "^7.1.1"
  }
}
